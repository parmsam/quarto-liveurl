---
title: "Attention Is All You Need"
subtitle: "Vaswani et al., 2017 — a slide summary"
format:
  revealjs:
    liveurl:
      button: true
      shortcut: "u"
      position: "right"
      width: "45vw"
      mode: "auto"
      sticky: true
      global:
        label: "Paper (HTML)"
        url: "https://arxiv.org/html/1706.03762v7"
revealjs-plugins:
  - liveurl
---

## About this deck

These slides summarise [*Attention Is All You Need*](https://arxiv.org/abs/1706.03762)
(Vaswani et al., NeurIPS 2017) — the paper that introduced the **Transformer**.

The full paper is always one keypress away:

- Press **`u`** to open it in the side panel
- The panel stays open as you advance through slides (`sticky` mode)
- Scroll to any section and your position is preserved

## The Problem {data-liveurl="https://arxiv.org/html/1706.03762v7#S1"}

Before Transformers, sequence modelling relied on **RNNs and LSTMs**.

**Key limitations:**

- $h_t$ depends on $h_{t-1}$ → inherently **sequential**, hard to parallelise
- Long sequences → vanishing gradients, early context is forgotten

> *"The dominant sequence transduction models are based on complex recurrent
> or convolutional neural networks."* — §1

Transformers replace recurrence with **attention over the full sequence**.

## Queries, Keys & Values {data-liveurl="https://arxiv.org/html/1706.03762v7#S3.SS2"}

Each token produces three learned vectors:

| Vector | Question it answers |
|--------|---------------------|
| **Q** (Query) | What am I looking for? |
| **K** (Key)   | What do I offer others? |
| **V** (Value) | What do I contribute if selected? |

Attention computes a weighted sum of Values, where the weights come from
matching each Query against all Keys.

## Scaled Dot-Product Attention {data-liveurl="https://arxiv.org/html/1706.03762v7#S3.SS2"}

$$\text{Attention}(Q, K, V) = \text{softmax}\!\left(\frac{QK^\top}{\sqrt{d_k}}\right)V$$

- $QK^\top$ — similarity between every query and every key
- $/ \sqrt{d_k}$ — scaling to prevent vanishing gradients from large dot products
- $\text{softmax}$ — normalise to a probability distribution over positions
- $\times V$ — weighted sum of value vectors

All positions are computed **in parallel** — no sequential dependency.

## Multi-Head Attention {data-liveurl="https://arxiv.org/html/1706.03762v7#S3.SS2.SSS2"}

Running attention once captures one type of relationship.

**Multi-head attention** runs it $h$ times with different projections:

$$\text{MultiHead}(Q,K,V) = \text{Concat}(\text{head}_1,\ldots,\text{head}_h)\,W^O$$

$$\text{head}_i = \text{Attention}(QW^Q_i,\; KW^K_i,\; VW^V_i)$$

The paper uses **$h = 8$ heads**, $d_k = d_v = 64$.

Each head specialises in a different relationship — syntax, coreference,
long-range dependencies — simultaneously.

## The Transformer Architecture {data-liveurl="https://arxiv.org/html/1706.03762v7#S3"}

::: {.liveurl}
- [Figure 1 — Architecture Diagram](https://arxiv.org/html/1706.03762v7#S3)
:::

An **encoder–decoder** design with 6 layers each.

Every layer wraps each sub-layer with a residual connection + layer norm:

$$\text{Output} = \text{LayerNorm}(x + \text{Sublayer}(x))$$

## Encoder {data-liveurl="https://arxiv.org/html/1706.03762v7#S3.SS1"}

Maps input tokens to continuous representations.

Each of the **6 encoder layers** applies:

```
token embeddings + positional encoding
         ↓
 Multi-Head Self-Attention → Add & Norm
         ↓
 Feed-Forward Network      → Add & Norm
```

Self-attention is **bidirectional** — every position sees the full sequence.

## Decoder {data-liveurl="https://arxiv.org/html/1706.03762v7#S3.SS1"}

Generates the output sequence one token at a time.

Each of the **6 decoder layers** applies:

```
output embeddings + positional encoding
         ↓
 Masked Self-Attention     → Add & Norm
         ↓
 Cross-Attention (encoder) → Add & Norm
         ↓
 Feed-Forward Network      → Add & Norm
```

**Masking** prevents position $i$ from attending to positions $> i$.

**Cross-attention** reads from the encoder's final representations.

## Positional Encoding {data-liveurl="https://arxiv.org/html/1706.03762v7#S3.SS5"}

Attention is **permutation-invariant** — no built-in notion of position.

Fixed sinusoidal encodings are added to the input embeddings:

$$PE_{(pos,\,2i)}   = \sin\!\left(\tfrac{pos}{10000^{2i/d}}\right)$$
$$PE_{(pos,\,2i+1)} = \cos\!\left(\tfrac{pos}{10000^{2i/d}}\right)$$

- Different frequencies encode different positional scales
- The model can learn to attend by **relative** offset
- Generalises to sequence lengths not seen during training

## Training {data-liveurl="https://arxiv.org/html/1706.03762v7#S5"}

- **Optimiser:** Adam with a warm-up learning rate schedule
- **Regularisation:** dropout 0.1 + label smoothing $\epsilon_{ls} = 0.1$
- **Hardware:** 8 × P100 GPUs

The base model trained in **12 hours**. Prior ensemble models took days.

## Results {data-liveurl="https://arxiv.org/html/1706.03762v7#S6"}

**WMT 2014 translation benchmarks:**

| Task | Transformer BLEU | Previous SOTA |
|------|:---:|:---:|
| English → German | **28.4** | 26.4 |
| English → French | **41.0** | 40.5 |

New state of the art on both tasks — at a fraction of the training cost.

## Why It Mattered

The Transformer is now the backbone of nearly every large model.

| Model | Year | Variant |
|-------|------|---------|
| BERT | 2018 | Encoder only |
| GPT series | 2018–present | Decoder only |
| T5 | 2019 | Encoder–decoder |
| ViT | 2020 | Encoder, image patches |
| LLaMA, Claude… | 2023–present | Decoder only |

Over **100,000 citations** as of 2024.
